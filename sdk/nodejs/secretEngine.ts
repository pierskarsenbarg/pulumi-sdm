// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * A SecretEngine is managing secrets in SecretStores.
 * This resource can be imported using the import command.
 *
 * ## Import
 *
 * A SecretEngine can be imported using the id, e.g.,
 *
 * ```sh
 * $ pulumi import sdm:index/secretEngine:SecretEngine example eng-12345678
 * ```
 */
export class SecretEngine extends pulumi.CustomResource {
    /**
     * Get an existing SecretEngine resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: SecretEngineState, opts?: pulumi.CustomResourceOptions): SecretEngine {
        return new SecretEngine(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'sdm:index/secretEngine:SecretEngine';

    /**
     * Returns true if the given object is an instance of SecretEngine.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is SecretEngine {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === SecretEngine.__pulumiType;
    }

    /**
     * ActiveDirectoryEngine is currently unstable, and its API may change, or it may be removed, without a major version bump.
     */
    public readonly activeDirectory!: pulumi.Output<outputs.SecretEngineActiveDirectory | undefined>;
    /**
     * KeyValueEngine is currently unstable, and its API may change, or it may be removed, without a major version bump.
     */
    public readonly keyValue!: pulumi.Output<outputs.SecretEngineKeyValue | undefined>;

    /**
     * Create a SecretEngine resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args?: SecretEngineArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: SecretEngineArgs | SecretEngineState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as SecretEngineState | undefined;
            resourceInputs["activeDirectory"] = state ? state.activeDirectory : undefined;
            resourceInputs["keyValue"] = state ? state.keyValue : undefined;
        } else {
            const args = argsOrState as SecretEngineArgs | undefined;
            resourceInputs["activeDirectory"] = args ? args.activeDirectory : undefined;
            resourceInputs["keyValue"] = args ? args.keyValue : undefined;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(SecretEngine.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering SecretEngine resources.
 */
export interface SecretEngineState {
    /**
     * ActiveDirectoryEngine is currently unstable, and its API may change, or it may be removed, without a major version bump.
     */
    activeDirectory?: pulumi.Input<inputs.SecretEngineActiveDirectory>;
    /**
     * KeyValueEngine is currently unstable, and its API may change, or it may be removed, without a major version bump.
     */
    keyValue?: pulumi.Input<inputs.SecretEngineKeyValue>;
}

/**
 * The set of arguments for constructing a SecretEngine resource.
 */
export interface SecretEngineArgs {
    /**
     * ActiveDirectoryEngine is currently unstable, and its API may change, or it may be removed, without a major version bump.
     */
    activeDirectory?: pulumi.Input<inputs.SecretEngineActiveDirectory>;
    /**
     * KeyValueEngine is currently unstable, and its API may change, or it may be removed, without a major version bump.
     */
    keyValue?: pulumi.Input<inputs.SecretEngineKeyValue>;
}
